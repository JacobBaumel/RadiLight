cmake_minimum_required(VERSION 3.15)
project(opencvTest CUDA)

set(STATIC_LIB_PATH "/home/nano/Radilight/nvapriltags/libapril_tagging.a")

find_package(OpenCV REQUIRED)
find_package(CUDA REQUIRED)

include_directories(${CUDA_INCLUDE_DIRS})
include_directories(${OpenCV_INCLUDE_DIRS})

cuda_add_executable(opencvTest main.cu)
target_link_libraries(opencvTest PRIVATE ${STATIC_LIB_PATH})
target_link_libraries(opencvTest ${CUDA_LIBRARIES} ${OpenCV_LIBS})
